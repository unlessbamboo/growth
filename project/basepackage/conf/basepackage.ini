[directory]
data={DATADIR}
logs=/data/logs/job

[pid]
aggregate=aggregate.pid
dubbo=dubbo.pid
mulAggregate=mulaggregate.pid
basic=basicmon.pid
java=javamon.pid
nginx=nginxmon.pid
tomcat=tomcatmon.pid
kv=kvmon.pid
usertrack=usertrack.pid
httpstatus=httpstaus.pid

[kafka]
kafka=kafka.ini

[java]
tracelog=tracing.log

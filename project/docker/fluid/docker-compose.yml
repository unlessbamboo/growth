version: '10'
services:
  blog:
    restart: "no"
    build:
        context: .
        dockerfile: Dockerfile
        # network: host
    image: blog:10
    container_name: myblog
    ports:
      - "4000:4000"
    volumes:  # 设置目录映射
     - $HOME/Public/blog:/usr/blog
